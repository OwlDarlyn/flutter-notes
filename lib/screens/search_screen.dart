import 'package:flutter/material.dart';

import '../widgets/bottom_nav.dart';

class SearchScreen extends StatelessWidget {
  const SearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('this is search');
    ;
  }
}
